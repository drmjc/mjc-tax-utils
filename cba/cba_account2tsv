#!/bin/bash
# Wrapper script to run cba_account2tsv.py with the correct venv
# Usage: cba_account2tsv path/to/Statement.pdf [--out output.tsv] [--debug]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
VENV_PYTHON="$REPO_ROOT/venv/bin/python"
SCRIPT="$REPO_ROOT/cba/cba_account2tsv.py"

exec "$VENV_PYTHON" "$SCRIPT" "$@"

